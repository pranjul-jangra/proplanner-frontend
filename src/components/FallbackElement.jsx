import React from 'react'
import '../styles/fallbackElement.css'

export default function FallbackElement() {
  return (

    // It's the fallback loader
    <section className='main-loader'>    
      <div></div>
    </section>
  )
}
